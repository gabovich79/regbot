# מבנה מסמכים ואבחון חילוץ דרישות

## שינוי שנבדק ונכנס לקוד

חילוץ Word מסמן תחילה וסוף של כל טבלה, לרבות טבלאות מקוננות. חלוקת המסמך שומרת את הטבלה כהקשר שלם ואינה הופכת מספרי סעיפים בתוך תאים לכותרות של חלקים אחרים. לכל טבלה תיאור מיקום הכולל את מספרה ואת השורה הראשונה כאשר היא קצרה מספיק. אין להסיק שתיאור זה הוא כותרת משפטית או תחולת כל התאים.

רשימת תוכן מפורשת שמכילה לפחות שלושה פריטי כותרת רצופים מסומנת לניווט. הפריטים נשארים בטקסט, אך אינם כותרות אב לגוף המסמך. הזיהוי שמרני ואינו מכסה את כל סוגי תוכן העניינים או פריסות PDF. נדרשת עדיין סקירת מבנה למקור חדש.

320 בדיקות עברו, כולל רשימה ממוספרת רגילה שאינה תוכן עניינים, טבלאות נפרדות ומקוננות, שימור טקסט ומעקב אחרי כשל ספק וביטול בקשה ללא retry. מקור מסמך 38 נבדק גם בכלי השוואת פסקאות: תוכן הפסקאות נשמר לאחר נרמול רווחים; זו אינה בדיקת נכונות משפטית או אישור חזותי מלא.

## הרצה מתוקנת

גרסת מסמך 38: `c76ece695c78409e82945e8a7875be6d`, 53 קטעים, 15 טבלאות. עלות אינדוקס $0.01812912. לא הופעלה בייצור.

שאלת ההשוואה בין מסלולים תלויי גיל למסלול כללי הסתיימה ב־45.55 שניות, בעלות $0.04405451. התשובה חלקית; היא מצטטת כעת מיקום טבלה ולא פריט מתוכן העניינים. אין ציון קבלה או אישור מקצועי.

מדידת קריאות JSON למודל: הבנה 1.67 שניות, ניווט 1.28, דירוג 6.23, כיסוי 5.50, חילוץ יחידות 8.42, ניסוח 4.19, אימות 7.48. אלו זמני ספק; הזמן הכולל כולל גם אחזור, embeddings ועיבוד מקומי. מדידת זמן ל־embeddings ולחיפוש רשת טרם נוספה.

## ניסוי ממוקד בכשל ההצטרפות

נעשה שימוש בראיות ובתכנית שנשמרו בהרצה הקודמת, ללא תשובת ייחוס וללא חיפוש חוזר. הדרישות חולקו לשלוש קבוצות, וכל קבוצה קיבלה את כל הראיות כדי לא להסתיר חריגים או הגדרות.

| קבוצה | זמן ספק | תוצאה |
|---|---:|---|
| 0, כולל דרישה כללית לפרטים והודעות | 23.88 שניות | 504, שמירת העלות נשמרה |
| 1, הודעות ממוקדות | 12.70 שניות | 7 יחידות שעברו בדיקת מבנה |
| 2, הודעות ממוקדות | 9.53 שניות | 6 יחידות שעברו בדיקת מבנה |

הקלטים דומים בגודלם, כ־49KB. זו אינדיקציה שהיקף המשימה או הפלט עשויים להיות רלוונטיים, לא הוכחה לגורם הכשל. הניסוי עלה $0.06876270 כולל שמירה בכשל. אין תשובה מלאה: קבוצה אחת חסרה, ואישור מבנה של 13 היחידות האחרות אינו מדד שלמות. החלוקה לא הוכנסה למסלול הרגיל.

השלב הבא הוא פירוק דרישות רחבות תוך כיסוי הנמען, הפרטים, המועדים, הבחירות והתנאים מהמקור, ולא הקטנת ראיות שרירותית או ניסיונות חוזרים ללא שינוי. עדיין נדרשת סקירת השינויים המסומנים במסמך 38 ותוקף המקורות.

תקציב מצטבר אחרי העבודה: $7.61048534 כולל $0.76471851 שמירות לא מסולקות, מתוך $20 מאושרים. ייצור לא השתנה.
